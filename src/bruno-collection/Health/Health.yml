info:
  name: Health
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{host-gql}}"
  body:
    query: |-
      query {
        health
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
