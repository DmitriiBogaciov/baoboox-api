info:
  name: LogoutAll
  type: graphql
  seq: 8

graphql:
  method: POST
  url: "{{host-gql}}"
  body:
    query: |-
      mutation {
        logoutAll
      }
    variables: |-
      {
        "input": {
          "sessionId": "6a345a67aee5fdd3be3634d7"
        }
      }
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: before-request
      code: |-
        const token = bru.getEnvVar('accessToken')
        req.setHeader('Authorization', `Bearer ${token}`)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
