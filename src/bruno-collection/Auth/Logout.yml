info:
  name: Logout
  type: graphql
  seq: 5

graphql:
  method: POST
  url: "{{host-gql}}"
  body:
    query: |-
      mutation{
        logout
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
